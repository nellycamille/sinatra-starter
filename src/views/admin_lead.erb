
  <div class="panel panel-default">
    <div class="panel-heading">
      <div class="pull-right" >

        <form class='form-inline' method="POST" action="/admin/leads/<%= @lead.id %>/change_status">
          <div class='form-group'>
            <label for="status">Estátus</label>
            <select id="status" class="form-control" name="status">
              <option value="0" <%= @lead.status == 0 ? "selected" : "" %>>Pendiente</option>
              <option value="1" <%= @lead.status == 1 ? "selected" : "" %>>Contactado</option>
            </select>
          </div>
          <button type="submit" class="btn btn-primary">
            <i class='fa fa-save'></i>
            Change
          </button>

        </form>


      </div>
      <h3 class="panel-title header-detailed">Datos del Cliente</h3>
    </div>
    <div class="panel-body panel-detailed">

        <table class="col-md-12">
          <tr>
            <th class="col-md-6">Nombre:</th>
            <th class="col-md-6"><%= @lead.name() %></th>
          </tr>
          <tr>
            <th class="col-md-6">Email:</th>
            <th class="col-md-6"><%= @lead.email() %></th>
          </tr>
          <tr>
            <th class="col-md-6">Servicio:</th>
            <th class="col-md-6"><%= @lead.servicio() %></th>
          </tr>
          <tr>
            <th class="col-md-6">Mensaje:</th>
            <th class="col-md-6"><%= @lead.message() %></th>
          </tr>
          <tr>
            <th class="col-md-6">Fecha del post:</th>
            <th class="col-md-6"><%= @lead.created_at() %></th>
          </tr>
      </table>

    </div>
  </div>


<!-- <select name='status' class='select'>
<% { "" => "All" , "0" => "Pending" , "1" => "Contacted" }.each do | key , value | %>
  <option value='<%= key %>' <%= params[:status] ==  key ? "selected" : "" %>><%= value %></option>
<% end %>
</select>
<div class="btn-group" role="group" aria-label="...">
  <button type="button" class="btn btn-default">Save</button>
</div> -->

<!-- <div class="input-group">
      <input type="text" class="form-control" placeholder="Search for...">
      <span class="input-group-btn">
        <button class="btn btn-default" type="button">Submit</button>
      </span>
</div><!-- /input-group -->

<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title header-detailed">Comments</h3>
  </div>
  <div class="panel-body panel-detailed">

    <form method="POST" action="">

      <textarea name="comments" class='form-control' rows='4'></textarea>
      <br />
      <div class='text-right'>
          <button class='btn btn-primary'>
            <i class='fa fa-plus'></i>
            Add Comment
          </button>
      </div>

      <hr />

      <div class="comments-list">
        <% @lead.comments.each do | comment | %>
         <div class="media">
              <p class="pull-right">
               <small><%= comment.created_at.in_time_zone("America/La_Paz").strftime("%d/%m/%Y %H:%M") %> </small>
               <a href="/admin/leads/comments/<%= comment.id %>" class="btn btn-danger"><i class='fa fa-trash'></i></a>
              </p>
              <div class="media-left">
                <i class='fa fa-user' style='font-size:40px'></i>
              </div>
              <div class="media-body">

                <h4 class="media-heading user_name"><%= comment.user.username %></h4>
                <%= comment.comment %>

              </div>
          </div>
          <% end %>
      </div>

    </form>

  </div>
</div>
